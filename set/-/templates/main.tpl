<div class="{__NODE_ID__}" instance="{__INSTANCE__}">

    <div class="fields">
        <!-- field -->
        <div class="field" number="{NUMBER}" field_id="{ID}">
            <div class="path" hover="hover">
                <div class="buttons" hover="hover">
                    {TYPE_SELECT}
                    {DUPLICATE_BUTTON}
                    {DELETE_BUTTON}
                </div>
                {PATH}
            </div>
            <div class="value">{VALUE}</div>
        </div>
        <!-- / -->
    </div>

    {CREATE_BUTTON}
    {COPY_BUTTON}
    {PASTE_BUTTON}
    {IMPORT_BUTTON}

</div>
